{
	"$schema": "https://json.schemastore.org/package.json",
	"author": {
		"email": "jeleniel@crystultima.org",
		"name": "JEleniel",
		"url": "https://github.com/JEleniel"
	},
	"dependencies": {},
	"devDependencies": {
		"@tailwindcss/cli": "^4.1.10",
		"prettier": "^3.6.0",
		"prettier-plugin-tailwindcss": "^0.6.13",
		"tailwindcss": "^4.1.10",
		"concurrently": "^9.2.0"
	},
	"license": "GPL-3.0-or-later",
	"name": "shiso-no-senshi",
	"private": true,
	"scripts": {
		"build": "dx clean && pnpm tailwind && dx build",
		"bundle": "dx clean && pnpm build && dx bundle",
		"dev": "dx clean && pnpm tailwind &&  dx serve --platform web",
		"dev:desktop": "dx clean && pnpm tailwind &&  dx serve --platform desktop",
		"tailwind": "pnpx @tailwindcss/cli -i ./assets/main.css -o ./assets/compiled.css -m"
	},
	"version": "0.1.0-alpha.1"
}
